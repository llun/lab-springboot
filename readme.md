# Springboot Lab with Kotlin

Personal Springboot lab with Kotlin

## How to run

- Install all dependencies with `mvn install`
- Use Java run output `.jar` in target directory e.g. `java -jar target/output.jar`